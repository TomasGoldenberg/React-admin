export const setUser = payload =>({
    type:"SET_USER",
    payload,
});


export const foundSale = payload =>({
    type:"FOUND_SALE",
    payload,
});

export const foundOrder = payload =>({
    type:"FOUND_ORDER",
    payload,
});

export const foundClient = payload =>({
    type:"FOUND_CLIENT",
    payload,
});

export const foundProduct = payload =>({
    type:"FOUND_PRODUCT",
    payload,
});

export const isSearching = payload =>({
    type:"IS_SEARCHING",
    payload,
});